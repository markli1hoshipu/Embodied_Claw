- resolve_model_ref can return step_exists=false even when the step is present (string "30000" vs int 30000 comparison quirk) — when found=true and the step appears in available_steps, confirm with list_local_models instead of treating it as missing; check_benchmark_compat is the real gate. Also: DOMINO pi05 eval entrypoint is policy/pi05/eval.sh (args: task_name task_config train_config_name model_name seed gpu_id); it sets XLA_PYTHON_CLIENT_MEM_FRACTION=0.4 itself, and deploy_policy.yml pins checkpoint_id (default 30000) — verify it matches the intended checkpoint_step.
- DOMINO eval_policy.py parses --overrides via argparse.REMAINDER into generic key/value pairs that update deploy_policy.yml's config — so the checkpoint_id: 30000 pin can be overridden per-run by appending '--checkpoint_id <step>' after --overrides, WITHOUT editing the shared yml (critical for A/B runs whose arms use different checkpoint steps, e.g. zeroshot step 0 vs finetuned 30000). eval.sh does NOT forward extra args ($@), so for non-default steps invoke 'python script/eval_policy.py --config policy/pi05/deploy_policy.yml --overrides ... --checkpoint_id <step>' directly from the repo root, replicating eval.sh's env (CUDA_VISIBLE_DEVICES, XLA_PYTHON_CLIENT_MEM_FRACTION=0.4, PYTHONWARNINGS=ignore::UserWarning, --ckpt_setting <model_name>).
